const mutations = {
  // 用时
  setTime(state, time) {
    state.time = time
  },
  // 昵称
  setNickname(state, nickname) {
    state.nickname = nickname
  },
  // 用户id
  setUserId(state, userId) {
    state.userId = userId
  },
}

export default mutations
